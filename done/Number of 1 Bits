var hammingWeight = function(n) {
      var m=n.toString(2).split("");
      var count=0;
      for (var i=0;i<m.length;i++){
          if(m[i]==="1")
          count++;
      }
      return count;
};
